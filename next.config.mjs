/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['firebasestorage.googleapis.com', 'cdn.imagin.studio', 'lh3.googleusercontent.com'],
  },
};

export default nextConfig;
